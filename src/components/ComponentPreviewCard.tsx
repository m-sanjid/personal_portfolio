import { motion } from "motion/react";

const ComponentPreviewCard = () => {
	return (
		<motion.div
			initial={{}}
			className="w-[350px] h-[360px] bg-white"
		></motion.div>
	);
};

export default ComponentPreviewCard;
